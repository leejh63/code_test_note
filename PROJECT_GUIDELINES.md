# LeetCode C 문제풀이 학습 프로젝트 지침서

## 1. 프로젝트 목적

LeetCode 문제의 정답 코드만 수집하지 않고 다음을 기록한다.

1. 내가 실제로 어떻게 접근했는지
2. 작성한 코드의 부족한 부분
3. 개선된 풀이와 그 이유
4. 풀지 못한 경우 막힌 원인과 필요한 학습
5. 실제로 질문한 내용과 답
6. 문서의 학습 및 수정 과정

---

# 2. 문제 상태

문제 상태는 다음 세 가지를 사용한다.

```text
SOLVED
UNSOLVED
RETRY
```

- `SOLVED`: 직접 풀이하여 동작하는 코드까지 작성
- `UNSOLVED`: 풀이 또는 구현을 완료하지 못함
- `RETRY`: 필요한 개념을 공부한 뒤 다시 도전 중

---

# 3. 날짜 기록 규칙

각 문제 문서 상단에는 반드시 다음 두 날짜를 기록한다.

```text
최초 작성일: YYYY-MM-DD
마지막 수정일: YYYY-MM-DD
```

## 최초 작성일

문서를 처음 만든 날짜이다.

한 번 기록한 뒤에는 변경하지 않는다.

## 마지막 수정일

문서를 실제로 수정한 가장 최근 날짜이다.

문서를 수정할 때마다 최신 날짜로 변경한다.

## 수정 이력

문서 하단에 다음 형식으로 기록한다.

```md
# 수정 이력

| 날짜 | 상태 | 변경 내용 |
|---|---|---|
| 2026-08-07 | UNSOLVED | 최초 작성 |
| 2026-08-08 | RETRY | 포인터 개념 학습 후 재도전 코드 추가 |
| 2026-08-10 | SOLVED | 최종 풀이 및 복잡도 정리 |
```

### 핵심 규칙

- 기존 수정 이력은 삭제하지 않는다.
- 이전 날짜를 새 날짜로 덮어쓰지 않는다.
- 수정할 때 새 행을 아래에 추가한다.
- 상단 `마지막 수정일`만 가장 최근 날짜로 갱신한다.
- 사소한 오탈자 수정까지 반드시 기록할 필요는 없다.
- 풀이, 코드, 질문, 상태, 학습 내용이 바뀌는 의미 있는 수정은 기록한다.

예:

```text
최초 작성일: 2026-08-07
마지막 수정일: 2026-08-10
```

수정 이력:

```text
2026-08-07  UNSOLVED  최초 작성
2026-08-08  RETRY     Hash Table 학습 내용 추가
2026-08-10  SOLVED    재도전 성공 및 최종 코드 추가
```

이렇게 하면 최초 학습 시점과 이후 발전 과정을 동시에 유지할 수 있다.

---

# 4. SOLVED 문제 작성 규칙

순서는 다음과 같다.

```text
간단 문제 설명
→ 내가 작성한 코드
→ 부족한 부분
→ 개선된 코드
→ 실제 질문과 답
→ 필요한 경우 부록
→ 수정 이력
```

사용자가 작성한 코드는 가능한 한 원본 그대로 보존한다.

코드 평가는 다음을 필요한 경우에만 확인한다.

- 정확성
- 논리 오류
- 경계 조건
- 중복 로직
- 불필요한 복잡성
- 시간/공간 복잡도
- C 언어 관련 문제

문제가 없는 항목을 억지로 단점으로 만들지 않는다.

---

# 5. UNSOLVED 문제 작성 규칙

사용자가 문제를 풀지 못했다고 명시하면 정답부터 제공하지 않는다.

진행 순서는:

```text
어디까지 시도했는지
→ 어디서 막혔는지
→ 부족한 개념
→ 필요한 학습
→ 단계별 힌트
→ 직접 재도전
→ 필요할 때 최종 풀이
```

UNSOLVED 문서는 상태가 바뀌어도 새 파일을 만들지 않는다.

예:

```text
2026-08-07  UNSOLVED
2026-08-08  RETRY
2026-08-10  SOLVED
```

같은 문제 문서 안에서 이어서 기록한다.

---

# 6. 질문과 부록

## 질문 사항과 답

사용자가 실제로 질문한 내용만 기록한다.

질문하지 않은 내용을 임의로 Q&A로 만들지 않는다.

## 부록

알아두면 좋지만 직접 질문하지 않았거나 본문 흐름에 필수적이지 않은 내용은 부록으로 분리한다.

필요하지 않으면 부록을 만들지 않는다.

---

# 7. 권장 GitHub 구조

초기에는 복잡하게 나누지 않는다.

```text
leetcode-c-study/
│
├── README.md
├── PROJECT_GUIDELINES.md
│
├── templates/
│   ├── solved_problem_template.md
│   └── unsolved_problem_template.md
│
└── problems/
    ├── 0001_two_sum.md
    ├── 0013_roman_to_integer.md
    └── ...
```

`SOLVED`, `UNSOLVED`, `RETRY`는 폴더가 아니라 각 문제 문서 내부의 상태값으로 관리한다.

---

# 8. 파일명 규칙

```text
문제번호_문제명.md
```

예:

```text
0001_two_sum.md
0013_roman_to_integer.md
0021_merge_two_sorted_lists.md
```

문제명은 소문자 `snake_case`를 사용한다.

날짜는 파일명에 넣지 않는다.

이유:

- 같은 문제를 수정할 때 파일명이 계속 바뀌지 않음
- Git 자체가 커밋 이력을 보존함
- 문제 문서 내부에서 최초 작성일과 수정 이력을 관리함

---

# 9. README 관리

README에는 전체 수정 이력을 넣지 않는다.

문제 목록과 현재 상태 정도만 관리한다.

예:

```md
| # | Problem | Status | Last Update |
|---|---|---|---|
| 1 | Two Sum | RETRY | 2026-08-08 |
| 13 | Roman to Integer | SOLVED | 2026-08-07 |
```

과거 수정 날짜는 개별 문제 문서의 `수정 이력`에서 확인한다.

---

# 10. 문서 수정 시 작업 순서

기존 문제 문서를 수정할 때:

```text
1. 기존 최초 작성일 유지
2. 본문 수정
3. 상단 마지막 수정일 변경
4. 하단 수정 이력에 새 행 추가
5. 상태가 바뀌었다면 상태도 함께 변경
6. README의 Status / Last Update 갱신
```

예:

```text
기존
상태: UNSOLVED
최초 작성일: 2026-08-07
마지막 수정일: 2026-08-07

↓ 학습 후 재도전

변경
상태: RETRY
최초 작성일: 2026-08-07
마지막 수정일: 2026-08-08
```

수정 이력에는:

```text
2026-08-07 | UNSOLVED | 최초 작성
2026-08-08 | RETRY    | 배열/포인터 학습 후 재도전 코드 추가
```

처럼 누적한다.

---

# 11. AI 작업 지침

1. 사용자의 기존 코드와 사고 과정을 먼저 보존한다.
2. 해결한 문제는 정확성을 먼저 검토한다.
3. 개선 이유가 성능인지, 구조인지, 가독성인지 구분한다.
4. 실제 질문만 Q&A에 기록한다.
5. 직접 묻지 않은 보충 설명은 부록으로 이동한다.
6. UNSOLVED 문제는 완성 정답을 바로 제공하지 않는다.
7. 막힌 지점과 부족한 개념을 먼저 분석한다.
8. 힌트는 단계적으로 제공한다.
9. 재도전한 사용자 코드를 우선 검토한다.
10. 기존 `최초 작성일`과 과거 `수정 이력`을 절대 삭제하거나 덮어쓰지 않는다.
11. 의미 있는 수정이 발생하면 `마지막 수정일`과 `수정 이력`을 갱신한다.

---

# 12. 완료 기준

## SOLVED

- [ ] 간단 문제 설명
- [ ] LeetCode 링크
- [ ] 내가 작성한 코드
- [ ] 부족한 부분
- [ ] 개선된 코드
- [ ] 복잡도
- [ ] 실제 질문이 있었다면 질문과 답
- [ ] 최초 작성일
- [ ] 마지막 수정일
- [ ] 수정 이력

## UNSOLVED

- [ ] 문제 설명
- [ ] 시도한 내용
- [ ] 막힌 지점
- [ ] 부족한 개념
- [ ] 학습 방향
- [ ] 단계별 힌트
- [ ] 최초 작성일
- [ ] 마지막 수정일
- [ ] 수정 이력
