# LeetCode C 문제풀이 학습 프로젝트 지침서

## 1. 프로젝트 목적

이 저장소는 LeetCode 정답 코드를 수집하는 저장소가 아니라,
C 언어로 문제를 풀면서 다음 내용을 기록하는 학습 저장소이다.

1. 내가 실제로 어떻게 문제에 접근했는지
2. 내가 작성한 코드의 장점과 부족한 부분
3. 참고 풀이에서 어떤 아이디어를 배웠는지
4. 학습 후 코드를 어떻게 개선했는지
5. 풀지 못한 문제에서 어디서 막혔는지
6. 실제 학습 중 생긴 질문과 답
7. 문제를 다시 학습하면서 어떤 내용이 변경되었는지

---

# 2. 문제 상태

각 문제는 다음 상태 중 하나를 사용한다.

```text
SOLVED
UNSOLVED
RETRY
```

- `SOLVED`: 직접 풀이하여 동작 가능한 코드까지 작성한 상태
- `UNSOLVED`: 풀이 방향 또는 구현을 완료하지 못한 상태
- `RETRY`: 부족한 개념을 공부한 뒤 다시 풀이 중인 상태

상태가 변경되어도 새 문제 파일을 만들지 않는다.
같은 문제 문서 안에서 계속 기록한다.

---

# 3. 날짜 기록 규칙

각 문제 문서 상단에는 다음을 기록한다.

```text
최초 작성일: YYYY-MM-DD
마지막 수정일: YYYY-MM-DD
```

## 최초 작성일

문서를 처음 만든 날짜이다.

한 번 기록한 후 변경하지 않는다.

## 마지막 수정일

문서를 의미 있게 수정한 가장 최근 날짜이다.

문서를 수정할 때 최신 날짜로 갱신한다.

## 수정 이력

문서 하단에 누적한다.

```md
# 수정 이력

| 날짜 | 상태 | 변경 내용 |
|---|---|---|
| 2026-08-07 | UNSOLVED | 최초 작성 |
| 2026-08-08 | RETRY | 포인터 개념 학습 후 재도전 |
| 2026-08-10 | SOLVED | 최종 풀이 정리 |
```

규칙:

- 기존 기록은 삭제하지 않는다.
- 이전 날짜를 새 날짜로 덮어쓰지 않는다.
- 의미 있는 수정이 생기면 새 행을 아래에 추가한다.
- 사소한 오탈자 수정까지 반드시 기록할 필요는 없다.

---

# 4. Public 저장소 콘텐츠 규칙

이 저장소는 Public으로 운영하는 것을 전제로 다음 기준을 적용한다.

## 4.1 문제 내용

LeetCode 문제 원문 전체를 복사하여 저장하지 않는다.

문제 문서에는 다음만 기록한다.

```text
문제 제목
내가 이해한 간단한 문제 설명
풀이에 필요한 핵심 조건
필요한 특수 규칙
LeetCode 원본 링크
```

## 4.2 내가 직접 작성한 코드

내가 직접 작성한 코드는 다음 항목에 그대로 기록한다.

```text
## 내가 진행한 코드 및 부족한 부분
```

가능하면 당시 작성한 코드를 보존한다.

## 4.3 LeetCode Solution 또는 다른 사용자 풀이를 참고한 경우

참고한 코드 전체를 문서에 그대로 복사하지 않는다.

다음 항목에 내가 이해한 **풀이 아이디어만** 정리한다.

```text
## 참고한 풀이에서 얻은 개선 아이디어
```

예:

```text
참고 풀이 A
- 뒤에서 앞으로 순회
- 현재 값과 오른쪽 값 비교
- 작은 값이면 빼기

참고 풀이 B
- 앞에서 뒤로 순회
- current < next이면 현재 값 빼기
```

필요하면 다음 정도의 출처 표시를 사용할 수 있다.

```text
출처: LeetCode Solution 참고
```

## 4.4 참고 후 다시 구현한 코드

참고 풀이의 원리를 이해한 뒤 직접 다시 구성한 코드는:

```text
## 개선된 코드
```

에 기록한다.

코드 앞에는 필요하면 다음처럼 구분한다.

```text
학습용 재구성 코드
```

목적은 참고 코드를 단순 복사하는 것이 아니라,
배운 원리를 이해한 뒤 내 코드로 다시 구현하는 것이다.

---

# 5. SOLVED 문제 작성 순서

SOLVED 문제는 다음 순서를 기본으로 한다.

```text
1. 간단 문제 설명

2. 내가 진행한 코드 및 부족한 부분
   └─ 내가 직접 작성한 코드

3. 참고한 풀이에서 얻은 개선 아이디어
   └─ 참고 코드 원문은 복사하지 않음
   └─ 내가 이해한 알고리즘/규칙만 정리

4. 개선된 코드
   └─ 학습 후 직접 재구성한 코드

5. 질문 사항과 답
   └─ 실제로 질문한 내용만

6. 부록
   └─ 직접 질문하지 않았지만 필요한 보충 내용
   └─ 필요 없으면 생략

7. 수정 이력
```

---

# 6. SOLVED 코드 검토 규칙

사용자가 직접 작성한 코드를 먼저 기준으로 분석한다.

필요한 경우 다음을 확인한다.

- 정상 동작 여부
- 논리 오류
- 경계 조건
- 중복 로직
- 불필요한 복잡성
- 가독성
- 시간 복잡도
- 공간 복잡도
- C의 포인터/배열/문자열/메모리 관련 문제

문제가 없는 항목을 억지로 단점으로 만들지 않는다.

예:

```text
시간 복잡도 O(n)으로 충분하다.
성능보다는 조건문 중복과 코드 구조를 단순화할 여지가 있다.
```

---

# 7. UNSOLVED 문제 작성 순서

사용자가 문제를 풀지 못했다고 명시하면 정답 코드부터 제공하지 않는다.

기본 순서:

```text
1. 간단 문제 설명

2. 어디까지 진행했는가
   └─ 시도한 코드
   └─ 생각한 접근
   └─ 막힌 지점

3. 부족한 개념 분석

4. 학습 방향

5. 단계별 힌트

6. 재도전
   └─ 학습 후 직접 다시 작성한 코드

7. 최종 풀이
   └─ 필요할 때만 작성

8. 질문 사항과 답

9. 부록

10. 수정 이력
```

---

# 8. UNSOLVED에서 참고 풀이를 확인하는 경우

정답을 확인하게 되더라도 참고 코드를 그대로 문제 문서에 저장하지 않는다.

기록 순서:

```text
참고 풀이에서 얻은 핵심 아이디어
→ 왜 이 아이디어가 필요한지 이해
→ 직접 재도전
→ 직접 작성한 코드 기록
```

최종 풀이를 작성할 때도:

- 참고 코드 원문 전체 복사 X
- 이해한 핵심 아이디어 O
- 학습 후 직접 재구성한 코드 O

를 기준으로 한다.

---

# 9. 질문 사항과 답

`질문 사항과 답`에는 실제 학습 중 사용자가 질문한 내용만 기록한다.

질문하지 않은 내용을 임의로 Q&A 형식으로 추가하지 않는다.

예:

```text
Q. M 앞에 I가 붙으면 current < next인데 빼면 안 되는 것 아닌가?
```

답변은 질문에 직접 대응하는 내용부터 작성한다.

---

# 10. 부록

다음과 같은 내용은 문제 본문에 필수적이지 않다면 부록으로 분리한다.

- C 문자열의 `'\0'`
- `strlen()`의 동작
- `switch`와 `if-else` 비교
- `malloc`, `free`
- 추가 자료구조 설명
- 다른 풀이 방식
- 반복해서 참고할 만한 구현 세부사항

필요한 내용이 없다면 부록을 만들지 않는다.

---

# 11. 복잡도 기록

최종 풀이에는 가능한 경우 다음을 기록한다.

```text
시간 복잡도: O(...)
공간 복잡도: O(...)
```

필요하면 한 줄 정도로 근거를 설명한다.

---

# 12. GitHub 디렉터리 구조

초기에는 복잡하게 나누지 않는다.

```text
code_test_note/
│
├── README.md
├── PROJECT_GUIDELINES.md
│
├── templates/
│   ├── solved_problem_template.md
│   └── unsolved_problem_template.md
│
└── problems/
    ├── 0001_two_sum.md
    ├── 0013_roman_to_integer.md
    └── ...
```

상태는 폴더가 아니라 각 문제 문서 내부에서 관리한다.

---

# 13. 파일명 규칙

```text
문제번호_문제명.md
```

예:

```text
0001_two_sum.md
0013_roman_to_integer.md
0021_merge_two_sorted_lists.md
```

문제명은 소문자 `snake_case`를 사용한다.

날짜는 파일명에 넣지 않는다.

---

# 14. README 관리

README에는 문제 목록과 현재 상태 정도만 기록한다.

예:

```md
| # | Problem | Status | First Written | Last Update |
|---|---|---|---|---|
| 13 | Roman to Integer | SOLVED | 2026-08-07 | 2026-08-07 |
```

상세한 수정 내역은 개별 문제 문서에서 확인한다.

---

# 15. 문제 문서 수정 순서

기존 문제 문서를 수정할 때:

```text
1. 기존 최초 작성일 유지
2. 본문 수정
3. 마지막 수정일 갱신
4. 수정 이력에 새 행 추가
5. 상태가 바뀌었다면 상태 변경
6. README의 Status / Last Update 갱신
```

---

# 16. AI 작업 지침

1. 사용자의 직접 작성 코드를 먼저 보존한다.
2. 해결한 문제는 사용자의 풀이를 먼저 검토한다.
3. 개선 이유가 성능, 구조, 가독성, 일반화 중 무엇인지 구분한다.
4. LeetCode 또는 다른 사용자의 참고 코드는 그대로 복사해 문서화하지 않는다.
5. 참고 풀이에서는 사용자가 이해해야 할 핵심 아이디어만 추출한다.
6. 개선 코드는 학습한 원리를 바탕으로 다시 구성한다.
7. 실제 질문만 Q&A에 기록한다.
8. 추가 설명은 부록으로 분리한다.
9. UNSOLVED 문제는 완성된 정답을 처음부터 제공하지 않는다.
10. 먼저 막힌 지점과 필요한 개념을 분석한다.
11. 힌트를 단계적으로 제공하고 직접 재도전을 우선한다.
12. 기존 최초 작성일과 수정 이력은 삭제하거나 덮어쓰지 않는다.

---

# 17. 완료 기준

## SOLVED

- [ ] 간단 문제 설명
- [ ] LeetCode 링크
- [ ] 내가 직접 작성한 코드
- [ ] 코드의 부족한 부분
- [ ] 참고 풀이가 있다면 아이디어만 정리
- [ ] 직접 재구성한 개선 코드
- [ ] 시간/공간 복잡도
- [ ] 실제 질문이 있었다면 질문과 답
- [ ] 최초 작성일
- [ ] 마지막 수정일
- [ ] 수정 이력

## UNSOLVED

- [ ] 간단 문제 설명
- [ ] 시도한 내용
- [ ] 막힌 지점
- [ ] 부족한 개념
- [ ] 학습 방향
- [ ] 단계별 힌트
- [ ] 최초 작성일
- [ ] 마지막 수정일
- [ ] 수정 이력
